<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    tools:context=".MasterDetailScreen.RecipeListFragment">

    <TextView
        android:id="@+id/recipe_ingredients_text"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:gravity="center"
        android:text="@string/ingredients_title"
        android:background="@color/colorDarkPeach"
        android:fontFamily="cursive"
        android:textColor="@color/colorWhite"
        android:textSize="55sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recipe_list_fragment_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="vertical"
        android:layout_below="@id/recipe_ingredients_text"/>

</RelativeLayout>